Paths:

# Get all users
/users

# Get information about {user_id}
/users/{user_id}

# Get all reviews from {user_id}
/users/{user_id}/reviews

# Get all coffee products
/coffee

# Get information about {coffee_id}
/coffee/{coffee_id}

# Get all reviews about {coffee_id}
/coffee/{coffee_id}/reviews

# Get all reviews
/reviews

# Register to make reviews
--- Returns a unique auth token for the user.
/register - name: <ditt namn> & email: <din email>

Authentication options:
--- Allowed option if valid token is sent in header.
# Post a review
/coffee/{coffee_id} - review: <din review>

# Delete a review
/users/{user_id}/reviews/{review_id}



User:
	id: integer
	name: string
	regtime: timestamp

User_auth:
	user_id: integer
	token: string
	email: string

Coffee_review:
	id: integer
	user_id: integer
	coffee_id: integer
	review: string
	timestamp: timestamp

# JSON-file from web scrape
Coffee:
	id: integer
	name: string
	url_info: string